
  # Context-Aware Handover UI

  This is a code bundle for Context-Aware Handover UI. The original project is available at https://www.figma.com/design/xSdn5vn94waavIUf0UAdAR/Context-Aware-Handover-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  